train = true

[data]
window_size    = 168
horizon_type   = "hour"
horizon_length = 24
label_len      = 0
batch_size     = 64   # unused by SARIMAX, kept for schema consistency

[training]
patience       = 0    # unused by SARIMAX
learning_rate  = 0.0  # unused by SARIMAX
train_epochs   = 0    # unused by SARIMAX

[model]
type = "sarimax"
