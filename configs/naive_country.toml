train = true
training_runs = 0   # 0 = single run with fixed seed for reproducibility

[experiment]
type = "country"    # "country" | "regions"

[data]
window_size    = 168    # 24*7 (1 week)
horizon_type   = "hour"
horizon_length = 24
label_len      = 0
batch_size     = 128

[training]
patience       = 50
learning_rate  = 0.00003
train_epochs   = 300

[model]
type = "linear_baseline2"
